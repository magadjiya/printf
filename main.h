#ifndef MAIN_H
#define MAIN_H

/* Header Files */
#include <unistd.h>
#include <stdarg.h>

/* Global Variables */

/* Function Declarations */
int _printf(const char *format, ...);
int other_specifiers(const char *format, ...);
int _strlen(char *s);
int _putchar(char c);

#endif /* MAIN_H */
